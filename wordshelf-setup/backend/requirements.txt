# ── WordShelf Backend Dependencies ──────────────────────────

# Web Framework (choose one — FastAPI recommended)
fastapi==0.111.0
uvicorn[standard]==0.29.0

# OR Flask alternative:
# flask==3.0.3
# flask-cors==4.0.1
# flask-jwt-extended==4.6.0

# Database
sqlalchemy==2.0.30
alembic==1.13.1

# MySQL driver (if using MySQL)
pymysql==1.1.0
cryptography==42.0.7

# OR MongoDB (if using MongoDB)
# pymongo==4.7.2
# motor==3.4.0           # async MongoDB driver

# Authentication
bcrypt==4.1.3
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0   # JWT
python-multipart==0.0.9

# Validation
pydantic==2.7.1
pydantic[email]==2.7.1
python-dotenv==1.0.1

# HTTP client (for external API calls)
httpx==0.27.0
requests==2.31.0

# NLP (vocabulary extraction)
spacy==3.7.4
# After install, run: python -m spacy download en_core_web_sm

# PDF Generation
reportlab==4.1.0
# OR: weasyprint==62.3

# AI / OpenAI
openai==1.26.0

# Utilities
python-slugify==8.0.4
Pillow==10.3.0             # image handling for avatar
email-validator==2.1.1

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0              # for FastAPI test client
